The Language qurts_grammar
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of qurts_grammar==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in qurts_grammar are the following:
  | ``as`` | ``bool`` | ``bot`` | ``copy``
  | ``drop`` | ``else`` | ``endlft`` | ``false``
  | ``fn`` | ``if`` | ``let`` | ``meas``
  | ``newlft`` | ``noop`` | ``qbit`` | ``qif``
  | ``top`` | ``true`` |  |

The symbols used in qurts_grammar are the following:
  | ; | , | ( | )
  | -> | : | < | |
  | > | <= | { | }
  | = | & | [ | ]
  | * | # |  |

===Comments===
There are no single-line comments in the grammar. There are no multiple-line comments in the grammar.

==The syntactic structure of qurts_grammar==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //[Function]// | -> | **eps**
  |  |  **|**  | //Function//
  |  |  **|**  | //Function// ``;`` //[Function]//
  | //[Param]// | -> | **eps**
  |  |  **|**  | //Param//
  |  |  **|**  | //Param// ``,`` //[Param]//
  | //[Lifetime]// | -> | **eps**
  |  |  **|**  | //Lifetime//
  |  |  **|**  | //Lifetime// ``,`` //[Lifetime]//
  | //[Ident]// | -> | **eps**
  |  |  **|**  | //Ident//
  |  |  **|**  | //Ident// ``,`` //[Ident]//
  | //[LifetimeConstraint]// | -> | **eps**
  |  |  **|**  | //LifetimeConstraint//
  |  |  **|**  | //LifetimeConstraint// ``,`` //[LifetimeConstraint]//
  | //Program// | -> | //[Function]//
  | //Function// | -> | ``fn`` //Ident// //Signature// //Block//
  | //Signature// | -> | //LifetimeContext// ``(`` //[Param]// ``)`` ``->`` //Type//
  | //Param// | -> | //Ident// ``:`` //Type//
  | //LifetimeContext// | -> | ``<`` //[Lifetime]// ``|`` //[LifetimeConstraint]// ``>``
  | //LifetimeConstraint// | -> | //Lifetime// ``<=`` //Lifetime//
  | //Block// | -> | ``{`` //Stmt// ``;`` //Ident// ``}``
  | //Stmt// | -> | //SimpleStmt//
  |  |  **|**  | //Stmt// ``;`` //SimpleStmt//
  | //SimpleStmt// | -> | ``noop``
  |  |  **|**  | ``newlft`` //Lifetime//
  |  |  **|**  | ``endlft`` //Lifetime//
  |  |  **|**  | //Lifetime// ``<=`` //Lifetime//
  |  |  **|**  | //Ident// ``as`` //Type//
  |  |  **|**  | ``let`` //Ident// ``=`` ``&`` //Lifetime// //Ident//
  |  |  **|**  | ``let`` //Ident// ``=`` //Expr//
  |  |  **|**  | ``let`` ``(`` //Ident// ``,`` //Ident// ``)`` ``=`` //Ident//
  |  |  **|**  | ``drop`` //Ident//
  | //Expr// | -> | //Ident//
  |  |  **|**  | ``true``
  |  |  **|**  | ``false``
  |  |  **|**  | ``(`` ``)``
  |  |  **|**  | ``(`` //Ident// ``,`` //Ident// ``)``
  |  |  **|**  | ``copy`` //Ident//
  |  |  **|**  | ``meas`` ``(`` //Ident// ``)``
  |  |  **|**  | //Ident// ``(`` //Ident// ``)``
  |  |  **|**  | ``[`` //Ident// ``]`` ``(`` //Ident// ``)``
  |  |  **|**  | //Ident// ``<`` //[Lifetime]// ``>`` ``(`` //[Ident]// ``)``
  |  |  **|**  | ``if`` //Ident// //Block// ``else`` //Block//
  |  |  **|**  | ``qif`` //Ident// //Block// ``else`` //Block//
  | //Type// | -> | //Type// ``*`` //PrefixType//
  |  |  **|**  | //PrefixType//
  | //PrefixType// | -> | ``&`` //Lifetime// //PrefixType//
  |  |  **|**  | ``#`` //Lifetime// //PrefixType//
  |  |  **|**  | ``bool``
  |  |  **|**  | ``qbit``
  |  |  **|**  | ``(`` ``)``
  | //Lifetime// | -> | //Ident//
  |  |  **|**  | ``bot``
  |  |  **|**  | ``top``



%% File generated by the BNF Converter (bnfc 2.9.6.3).
